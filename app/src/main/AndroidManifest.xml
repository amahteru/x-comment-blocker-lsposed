<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".XCommentBlockerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XCommentBlocker">

        <!-- Xposed Module Metadata -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/xposed_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedsharedprefs"
            android:value="true" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.KeywordEditorActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ContentProvider for cross-process preferences sharing with LSPosed/Xposed -->
        <provider
            android:name=".data.ConfigProvider"
            android:authorities="com.xtwitter.blocker.config"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <!-- Receiver for blocked count updates -->
        <receiver
            android:name=".data.BlockedCountReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xtwitter.blocker.ACTION_INCREMENT_BLOCKED" />
            </intent-filter>
        </receiver>

        <!-- Receiver for cross-process configuration queries -->
        <receiver
            android:name=".data.ConfigQueryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xtwitter.blocker.ACTION_GET_CONFIG" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
